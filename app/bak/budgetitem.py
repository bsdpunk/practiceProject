class BudgetItem

def __init__(self, price, vendor, budgetItem, previousBalance)
    self.price = price
    self.vendor = vendor
    self.budgetItem = budgetItem
    self.previousBalance = previousBalance

def currentBalance(self)
    return previousBalance - price
